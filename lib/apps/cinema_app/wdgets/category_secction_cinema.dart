import 'package:flutter/material.dart';

class CategorySecctionCinema extends StatelessWidget {
  const CategorySecctionCinema({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
